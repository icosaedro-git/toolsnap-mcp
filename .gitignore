node_modules
.wrangler
dist
.dev.vars
.env.local
.env
.DS_Store
*.log
registry-key.pem

# Fase 21.1 — Astro site: generated data (regenerated from src/ before every
# dev/build, see site/scripts/generate-data.mts) and Astro's own cache.
site/src/data/*.json
site/.astro
