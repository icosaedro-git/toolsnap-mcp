# Smithery registry entry — https://smithery.ai
# Submit via: https://smithery.ai/new or GitHub PR to smithery-ai/registry
# This file is for reference; fill the web form with these values.

name: toolsnap-mcp
displayName: "ToolSnap MCP — Context-Efficient Microtools"
description: |
  MCP server for AI agents. 38 tools total, 35 free — web extraction, SEO metadata,
  documents (PDF/CSV/JSON), feeds/sitemaps, link checking, plus utilities (UUID, hash,
  Base64, URL encode/decode, HTML→Markdown, diff, regex, token count, and more).
  Flagship fetch_extract: median 98.1% token reduction (53 820 → 2 001 tokens, 11 real
  pages). Saves $0.156/call at Sonnet pricing; ROI positive even at DeepSeek pricing with
  $0.01 prepaid (deposit once, debit off-chain, no per-call gas). Pay per call $0.02 USDC
  on Base via x402, first call free per wallet.

homepage: https://mcp.toolsnap.app
repository: https://github.com/icosaedro-git/toolsnap-mcp

transport:
  type: streamable-http
  url: https://mcp.toolsnap.app/mcp

serverCard: https://mcp.toolsnap.app/.well-known/mcp.json
pricingEndpoint: https://mcp.toolsnap.app/.well-known/pricing.json

categories:
  - developer-tools
  - web-scraping
  - ai-agents
  - paid

pricing:
  model: per-call
  currency: USDC
  network: base
  pricePerCall: 0.02
  freeTools: 35
  prepaidPricePerCall: 0.01
  minDeposit: 0.50
  firstCallFree: true

tags:
  - mcp
  - x402
  - usdc
  - base
  - fetch
  - token-reduction
  - context-efficient
  - cloudflare-workers

author:
  name: Icosaedro.one
  url: https://mcp.toolsnap.app
